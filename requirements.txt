# Web Crawling & Scraping
aiohttp>=3.9.0
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Bloom Filter for deduplication
pybloom-live>=4.0.0

# Lightweight token estimation (no tokenizer model dependency)
#pip uninstall -y transformers tokenizers huggingface-hub safetensors ( no need to run this)


# LLM Client (local inference via llama.cpp)
openai>=1.10.0

# BM25 lexical search (Layer 4 fallback)
rank_bm25>=0.2.2

# API Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
